<!DOCTYPE html>
<html>
  <head>
    {% if title %}
    <title>{{ title }} - SDN</title>
    {% else %}
    <title>Welcome to AUTOS</title>
    {% endif %}
	<style>
        body {
            background:white;
            color:blue;
        }

    </style>
  </head>
  <body>

<div>
{% with messages = get_flashed_messages() %}
      {% if messages %}
        <ul>
        {% for message in messages %}
            <li style="color:green">{{ message }} </li>
        {% endfor %}
        </ul>
      {% endif %}
    {% endwith %}
	
	<h1> Informations sur slice</h1>
		{% if tranche is iterable and tranche is not string%}
		<p> Infos sur le slice <b style=colorred>{{ tranche["slice-name"] }}</b></p>
			<ul>
				{% for k,v in tranche.items() %}
				{% if k!="slice-name" %}
				<li>{{k}}: <b>{{v}}</b></li>
				{% endif %}		
				{% endfor %}
			
		{% else %}
			<b style="color:red">{{tranche|lower }}</b>
		{% endif %}
	</ul>
<div>
	<a href="{{url_for('index')}}">retour</a>
	<a href="{{url_for('updateRequestSlice',nom=tranche['slice-name'])}}">Update</a>
	<a href="{{url_for('index',nom=tranche['slice-name'],conf=0)}}">Delete</a>
</div>

</div>
  </body>
</html>
